import { ReviewProps } from "../interfaces/ComponentProps";
import { StyledReview } from "../src/styles/styledComponents/StyledReview";

const Review = (props: ReviewProps) => {
  return <StyledReview></StyledReview>;
};

export default Review;
